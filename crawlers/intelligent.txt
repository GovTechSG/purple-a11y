

CRAWL-INTELLIGENT-SITEMAP
    
    if (sitemap dont exist){

        run crawlDomain 

    } else {

        arrange sitemap: urls starting from user input url to the top, then arrange via descending date

        run crawlSitemap
        
        if (scanned urls < p){
            run crawl Domain starting from url that user input
        }

    }





CRAWLDOMAIN

    requestHandler:
        if request.url has been scanned before, dont scan this page



FLAGS

-c 2 crawlIntelligentSitemap
-c 5 crawlDomain
updated node cli.js --help
updated readme






